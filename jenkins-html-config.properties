# Jenkins HTML Publisher Configuration
# This file contains settings to help with HTML report publishing

# Content Security Policy settings for HTML reports
# Add these to Jenkins system configuration if needed:
# hudson.model.DirectoryBrowserSupport.CSP=sandbox allow-same-origin allow-scripts allow-forms allow-top-navigation

# Alternative minimal CSP that should work:
# hudson.model.DirectoryBrowserSupport.CSP=default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'

# For complete functionality (use with caution in production):
# hudson.model.DirectoryBrowserSupport.CSP=
